# 科研人员知识图谱分析系统 — 系统架构和功能设计

> 版本：v1.1（当前实现版本）
> 文档日期：2026年9月18日

---

## 一、系统概述

本系统是面向高校内部管理使用的**科研人员画像与知识图谱分析平台**，旨在通过知识图谱技术将科研人员、论文、专利、项目、机构之间的关联关系可视化，辅助管理层进行人才发现、学科评估和机构竞争力分析。

### 1.1 系统定位

- **使用对象**：校级管理人员、院系管理人员、科研管理部门
- **核心价值**：将分散的科研数据整合为可交互的知识图谱，支持多维度查询和分析
- **数据范围**：校内科研人员、发表论文、申请专利、主持/参与项目、所属院系

---

## 二、技术架构

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                      前端层（单页应用）                    │
│   HTML5 + ECharts 5.4.3 + 原生 JavaScript               │
│   访问地址: http://localhost:8089/portrait/              │
└─────────────────────┬───────────────────────────────────┘
                       │ HTTP REST API
┌─────────────────────▼───────────────────────────────────┐
│                   后端层（Spring Boot）                   │
│   Spring Boot 3.2.5 + Java 21                           │
│   端口: 8089  上下文路径: /portrait                       │
│                                                          │
│   Controller层  →  Service层  →  Repository层            │
└──────────┬──────────────────────────┬───────────────────┘
           │                          │
┌──────────▼──────────┐  ┌───────────▼──────────────────┐
│   MySQL 8.x         │  │   Neo4j 5.x                  │
│   （关系型主库）      │  │   （图数据库）                 │
│   科研人员基础数据    │  │   知识图谱节点与关系           │
│   论文/专利/项目数据  │  │   Researcher/Paper/Patent    │
│   影响力权重配置      │  │   Project/Institution 节点   │
└─────────────────────┘  └──────────────────────────────┘
```

### 2.2 技术栈清单

| 层次 | 技术 | 版本 | 用途 |
|------|------|------|------|
| 前端 | HTML5 / CSS3 / JavaScript | — | 单页应用 |
| 前端 | ECharts | 5.4.3 | 图谱、雷达图、趋势图等可视化 |
| 后端框架 | Spring Boot | 3.2.5 | 应用框架 |
| 图数据库ORM | Spring Data Neo4j | 7.x | Neo4j 访问层 |
| 关系数据库ORM | MyBatis | — | MySQL 访问层 |
| 图数据库 | Neo4j | 5.x | 知识图谱存储 |
| 关系数据库 | MySQL | 8.x | 业务数据存储 |
| 构建工具 | Maven | — | 依赖管理与构建 |
| 定时任务 | Spring Scheduling | — | 影响力指数定期刷新 |

### 2.3 数据流设计

```
MySQL（原始业务数据）
        │
        │  DataSyncService.syncAllToNeo4j()
        ▼
Neo4j（知识图谱）
        │
        │  GraphService / ResearcherService
        ▼
REST API（JSON）
        │
        │  HTTP
        ▼
前端 ECharts 可视化
```

---

## 三、数据模型

### 3.1 Neo4j 节点类型

| 节点类型 | 中文名 | 主要属性 | 图谱颜色 |
|---------|--------|---------|---------|
| `Researcher` | 科研人员 | name, title, department, researchField, disciplineCategory, influenceIndex | 蓝色 |
| `Institution` | 院系/机构 | instName, name, type, parentId | 绿色 |
| `Paper` | 论文 | title, journal, pubDate, citedNum, impactFactor, subjectField, international（是否国际合作） | 橙色/金色 |
| `Patent` | 专利 | name, patentNo, patentType, applyDate, grantDate, status, transferred（是否转化）, transferAmount（转化金额） | 红色 |
| `Project` | 项目 | projName, projLevel, fund, startDate, endDate, source, status | 紫色 |

### 3.2 节点关系类型

| 关系类型 | 方向 | 含义 |
|---------|------|------|
| `BELONG_TO` | Researcher → Institution | 科研人员隶属于某院系 |
| `WRITE` | Researcher → Paper | 科研人员发表论文 |
| `INVENT` | Researcher → Patent | 科研人员申请专利 |
| `CHARGE` | Researcher → Project | 科研人员负责某项目 |
| `PARTICIPATE` | Researcher → Project | 科研人员参与某项目 |
| `COOPERATE_WITH` | Researcher ↔ Researcher | 科研人员之间的合作关系（无向） |
| `CITES` | Paper → Paper | 论文引用关系 |

### 3.3 院系二级结构

机构（Institution）支持两级层次结构：

- **一级**：学院、独立系、研究所（`parentId = null`），图谱节点尺寸较大（44px）
- **二级**：学院下属的系或研究所（`parentId = 上级机构id`），图谱节点尺寸较小（22px）

### 3.4 MySQL 数据表

| 表名 | 说明 |
|------|------|
| `researcher` | 科研人员基础信息 |
| `institution` | 机构/院系信息 |
| `paper` | 论文信息 |
| `patent` | 专利信息 |
| `project` | 项目信息 |
| `weight_config` | 学科影响力权重配置 |

---

## 四、功能模块设计

### 4.1 知识图谱视图（主视图）

#### 4.1.1 全量图谱展示

- 从 Neo4j 读取所有节点和关系，使用 ECharts force-layout 渲染
- 节点按类型用颜色区分，支持图例显示/隐藏
- 一级院系节点（44px）与二级院系节点（22px）尺寸不同

#### 4.1.2 图谱交互

| 操作 | 功能 |
|------|------|
| 单击科研人员节点 | 打开个人画像弹窗 |
| 双击任意节点 | 展开该节点的所有邻居节点 |
| 鼠标悬停节点 | 显示节点详细属性 tooltip |
| 滚轮缩放 | 缩放图谱 |
| 拖拽节点 | 移动节点位置 |
| 拖拽背景 | 平移图谱画布 |

#### 4.1.3 图谱筛选面板（左侧浮动）

**院系筛选**

- 下拉列表支持二级层次结构（optgroup 分组）
- 有下属系的一级院系可选择"全部"（含所有下属二级系）
- 筛选逻辑：通过 `BELONG_TO` 关系边查找科研人员，并展开其关联的论文/专利/项目

**分类关键词检索**（可开关）

- 默认关闭；通过右上角 Toggle Switch 开启
- 支持检索类别：科研人员 / 论文 / 专利 / 项目
- 支持多关键词（空格分隔）
- 匹配模式：AND（全部匹配） / OR（任一匹配）
- 筛选结果信息实时显示（节点数 / 关系数）

#### 4.1.4 全局搜索

- 顶部搜索框，输入科研人员姓名或院系名称
- 定位到图谱中对应节点并高亮

### 4.2 个人画像弹窗

点击图谱中科研人员节点后弹出，包含以下信息：

#### 4.2.1 基本信息卡片

| 字段 | 说明 |
|------|------|
| 所属院系 | 该科研人员的院系归属 |
| 职称/职务 | 教授/副教授/讲师/研究员等 |
| 研究方向 | 核心研究领域 |
| 学术影响力指数 | 系统计算的综合影响力得分 |
| 累计项目经费 | 主持项目的总经费（万元） |

#### 4.2.2 智能标签与项目层级分布

- 智能标签体系：基于项目、论文、专利等多源成果数据自动推理，覆盖 5 个维度（研究方向 / 学术影响力 / 科研产出 / 项目层级 / 合作交流），以彩色标签块展示
- 项目层级分布：以统计卡展示（主持 + 参与）项目在国家级 / 省部级 / 横向三个层级的数量分布

#### 4.2.3 产出趋势标签页

- 折线图（论文）+ 柱状图（专利）
- 按年度展示科研产出数量变化趋势

#### 4.2.4 主题演变标签页

- 以力导向图展示各年度研究关键词
- 展示研究方向随时间的演变脉络
- 主线连接各年度第一关键词节点

#### 4.2.5 合作网络标签页

- 核心合作者关系图（Top 5 合作者）
- 合作者列表（含合作频次）
- 跨学科合作统计信息
- "在主图谱中探查二级合作网络"：展开一级 + 二级合作者，并叠加网络分析结果——
  - **核心枢纽学者识别**：基于全图合作网络度数中心度，度数 ≥ max(平均度数×1.5, 8) 的 Top 20 学者标记为枢纽（金色描边高亮，枢纽指数 = 度数 / 最大度数 × 100）
  - **学术社群检测**：基于标签传播算法（LPA，固定随机种子保证确定性），社群成员在图谱中按颜色区分
  - 节点 tooltip 展示网络度、枢纽指数与社群归属

#### 4.2.6 智能分析报告

- 点击"下载 PDF 报告"按钮：服务层先生成 Markdown 文本，再经 PDF 渲染工具类转换为 PDF 后下载
- 报告内容：基本情况、学术影响力分析、科研产出分析、合作网络分析、发展建议

### 4.3 人才发现视图

#### 4.3.1 多条件筛选

| 筛选条件 | 选项 |
|---------|------|
| 职称级别 | 教授 / 副教授 / 讲师 / 研究员 / 副研究员 / 助理教授 |
| 学科门类 | 工学 / 理学 / 医学 / 管理学 / 人文社科 |
| 影响力指数下限 | 数值输入 |
| 是否国家级项目负责人 | 复选框 |

#### 4.3.2 结果展示

- 表格形式展示：序号、姓名、职称、所属院系、研究方向、影响力指数
- 支持按影响力指数、姓名等列排序（升序/降序切换）
- 显示筛选结果总数

#### 4.3.3 数据导出

- "导出筛选结果"按钮：导出当前筛选结果为 Excel（.xlsx）
- "导出报表"按钮（顶部）：导出全量科研人员数据为 Excel

### 4.4 院系对标视图

#### 4.4.1 机构列表

- 支持搜索过滤院系名称
- 支持全选 / 全不选
- 树形层次展示（一级学院 + 二级系）
- 显示机构总数

#### 4.4.2 竞争力雷达图对比（7 维）

选择多个院系后点击"生成对比报告"，展示以下 7 个维度的雷达图（各维度归一化到 0~100）：

| 维度 | 计算方式 |
|------|---------|
| 科研规模 | (科研人员数 + 论文数 + 专利数 + 项目数) ÷ 10 |
| 科研质量 | 平均影响因子 × 10 + 国家级项目数 × 5 |
| 学术影响力 | 平均影响力指数 × 10 |
| 科研效率 | (论文数 + 专利数) ÷ 科研人员数 × 5 |
| 学术合作 | min(人均合作者数×5, 60) + min(跨机构合作占比%×0.8, 40) |
| 国际合作 | min(国际合作论文占比%×2.5, 100) |
| 成果转化 | min(专利转化率%×0.5 + 已转化专利数×5, 100) |

机构聚合数据通过 Cypher CALL 子查询按维度分别聚合（避免笛卡尔积膨胀）。

#### 4.4.3 维度钻取与溯源

- 雷达图下方提供维度按钮（7 个）与钻取机构下拉框
- 点击维度后调用钻取接口，返回：计算方式说明（formula）、二级指标（level2）与原始成果清单（items，最多 20 条）
- 前端以弹出面板展示，确保分析结果可解释、可追溯

#### 4.4.4 竞争力分析简报生成

- "生成分析简报（PDF）"按钮：服务层基于对标结果自动生成 Markdown 简报 → PdfExportUtil 渲染为 PDF 下载
- 简报内容：综合评估概览（7 维得分表）、相对优势领域（含数据支撑）、明显短板领域、结论与建议
- 列表中第一个选中的机构作为"主机构（本校）"

#### 4.4.5 文字分析摘要

雷达图下方自动生成各机构竞争力的文字描述摘要。

#### 4.4.6 影响力刷新

- "刷新影响力指数"按钮：触发全量重新计算所有科研人员的学术影响力指数

---

### 4.5 群体对比视图

#### 4.5.1 群体划分维度

支持按学科领域（discipline）/ 职称级别（title）/ 所属院系（department）/ 人才类别（talentCategory）四个预设维度划分群体。

人才类别为派生维度：影响力指数 ≥85（或教授/研究员且主持国家级项目）→ 领军人才；≥65 → 学术骨干；其余 → 青年学者。

#### 4.5.2 聚合指标

按群体聚合：人数、论文数、专利数、项目数、经费合计、平均影响力、人均论文/专利/项目、人均合作者数（合作广度）、国际合作论文数、转化专利数，以及按年度的论文产出趋势（byYear）。

#### 4.5.3 可视化

- 柱状图：所选指标的各群体横向对比（支持 9 种指标切换）
- 折线图：各群体年度论文产出趋势对比（最多 12 个群体）
- 侧边汇总：群体数量、覆盖人数、选定指标最高群体

---

## 五、学术影响力指数算法

### 5.1 计算公式

影响力指数由论文、专利、项目三部分加权求和，并对时间进行衰减处理：

```
影响力指数 = Σ(论文得分 × 衰减系数 × 论文权重)
           + Σ(专利得分 × 衰减系数 × 专利权重)
           + Σ(项目得分 × 衰减系数 × 项目权重)
```

### 5.2 各类得分计算

| 类别 | 基础得分计算 |
|------|------------|
| 论文 | 被引次数 × 0.5 + 影响因子 × 2.0 |
| 发明专利 | 基础分 10.0 |
| 其他专利 | 基础分 5.0 |
| 项目 | 经费（万元）× 0.1；国家级项目额外 × 1.5 |

### 5.3 时间衰减函数

```
W(t) = W₀ × e^(-λ × t)

其中：
  W₀ = 基础得分
  λ  = 衰减系数（默认 0.05）
  t  = 距今年数
```

### 5.4 学科权重配置（默认值）

| 学科 | 论文权重 | 专利权重 | 项目权重 |
|------|---------|---------|---------|
| 工学 | 0.3 | 0.4 | 0.3 |
| 理学 | 0.6 | 0.1 | 0.3 |
| 默认/人文社科 | 0.5 | 0.1 | 0.4 |

> 权重配置支持通过数据库 `weight_config` 表按学科自定义。

---

## 六、REST API 接口清单

### 6.1 图谱接口（/api/graph）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/graph/full` | 获取全量知识图谱（节点+关系） |
| GET | `/api/graph/expand/{nodeId}` | 双击展开：获取指定节点的邻居 |
| GET | `/api/graph/cooperation/{id}` | 获取科研人员合作网络分析数据 |
| GET | `/api/graph/cooperation-network/{id}` | 二级合作网络探查（含枢纽识别与社群检测结果） |

### 6.2 科研人员接口（/api/researcher）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/researcher/all` | 查询所有科研人员 |
| GET | `/api/researcher/get/{id}` | 按ID查询科研人员 |
| GET | `/api/researcher/profile/{id}` | 获取个人画像详情（含趋势、主题演变等） |
| POST | `/api/researcher/search` | 高级人才筛选 |
| GET | `/api/researcher/export` | 导出全量科研人员 Excel |
| POST | `/api/researcher/export-search` | 导出筛选结果 Excel |
| GET | `/api/researcher/report/{id}` | 下载智能分析报告（PDF，内嵌中文字体） |
| POST | `/api/researcher/refresh-influence/{id}` | 刷新单人影响力指数 |
| POST | `/api/researcher/refresh-influence/all` | 全量刷新影响力指数 |

### 6.3 机构接口（/api/institution）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/institution/all` | 获取院系树形结构（含二级层次） |
| POST | `/api/institution/compare` | 多机构竞争力雷达图对比 |
| GET | `/api/institution/radar/{id}` | 单机构竞争力数据 |
| GET | `/api/institution/drill/{id}?dimension=xxx` | 雷达维度钻取（二级指标 + 计算方式 + 原始成果清单） |
| GET | `/api/institution/brief-pdf?ids=...` | 机构竞争力分析简报 PDF 下载 |

### 6.4 分析接口（/api/analysis）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/analysis/group-compare?dimension=discipline` | 多维度群体对比聚合（含年度趋势） |

---

## 七、系统配置

### 7.1 运行环境要求

| 组件 | 要求 |
|------|------|
| JDK | 21（当前实现基于 21 构建） |
| MySQL | 8.x |
| Neo4j | 5.x（Bolt 协议，端口 7687） |
| 网络 | 前端通过 CDN 加载 ECharts，需联网 |

### 7.2 关键配置项（application.yaml）

```yaml
server:
  port: 8089
  servlet:
    context-path: /portrait

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/research_portrait
  neo4j:
    uri: bolt://localhost:7687
```

### 7.3 定时任务

| 任务 | 触发方式 | 说明 |
|------|---------|------|
| 影响力指数刷新 | Spring Scheduling（定时） + 手动触发 | 全量重新计算所有科研人员的影响力指数 |

---

## 八、项目目录结构

```
src/main/
├── java/com/jlvtc/researchportrait/
│   ├── controller/         # REST 控制器
│   │   ├── GraphController.java
│   │   ├── ResearcherController.java
│   │   ├── InstitutionController.java
│   │   ├── AnalysisController.java
│   │   ├── PaperController.java
│   │   ├── PatentController.java
│   │   └── ProjectController.java
│   ├── service/            # 业务逻辑层
│   │   ├── GraphService.java
│   │   ├── ResearcherService.java
│   │   ├── InstitutionService.java
│   │   ├── GroupAnalysisService.java
│   │   ├── InfluenceCalculatorService.java
│   │   ├── InfluenceScheduler.java
│   │   └── DataSyncService.java
│   ├── entity/             # 数据实体（Neo4j 节点类）
│   │   ├── Researcher.java
│   │   ├── Institution.java
│   │   ├── Paper.java
│   │   ├── Patent.java
│   │   ├── Project.java
│   │   └── WeightConfig.java
│   ├── repository/         # 数据访问层
│   ├── util/               # 工具类（ExcelExportUtil、PdfExportUtil）
│   └── common/             # 通用类（GraphResult、GraphNode 等）
└── resources/
    ├── static/
    │   └── index.html      # 前端单页应用（全部 UI 逻辑）
    ├── db/
    │   ├── schema.sql       # MySQL 建表脚本
    │   ├── test-data.sql    # MySQL 测试数据
    │   └── neo4j-test-data.cypher  # Neo4j 测试数据
    └── application.yaml    # 系统配置
```

---

*本文档描述系统当前版本（v1.1）的实际实现状态，如有功能扩展请同步更新本文档。*
