# 科研人员画像分析系统 — 生产环境配置资料与发布手册

> 版本：v1.1
> 文档日期：2026年9月18日
> 适用系统版本：research-portrait 1.0.0
> 适用读者：系统管理员 / 运维人员 / 信息中心技术人员
> 生产访问地址：http://<服务器IP>:8089/portrait/

---

## 一、系统概述与组件架构

### 1.1 系统定位

本系统是一套面向高校科研管理的科研人员画像与知识图谱分析平台，核心功能包括：知识图谱可视化、科研人员多维画像（智能标签/产出趋势/主题演变/合作网络及枢纽社群分析）、高端人才发现、多维度群体对比、院系竞争力对标（7 维雷达 + 维度钻取 + 竞争力简报 PDF）、Excel 报表与 PDF 分析报告导出。

### 1.2 技术栈与版本矩阵（生产环境必须与下表一致）

| 组件 | 版本 | 说明 |
|------|------|------|
| JDK | **21**（Oracle/OpenJDK 均可） | 应用运行环境，**不支持 Java 8/11/17** |
| Spring Boot | 3.2.5 | 应用框架（内嵌 Tomcat 10.1.20） |
| Spring Data Neo4j | 7.2.5 | 图数据库访问层 |
| **Neo4j** | **5.x**（5.26 已验证） | 图数据库，**系统唯一必需的数据库** |
| MySQL | 8.x（可选） | 权威数据源（设计保留，当前版本运行不依赖，见 4.3 节） |
| 前端 | 内嵌静态页面（ECharts） | 无需独立部署，已打包在应用 jar 内 |

### 1.3 架构说明

```
┌─────────────┐     HTTP/8089      ┌──────────────────┐
│  浏览器客户端  │ ───────────────→ │  research-portrait  │
│ (ECharts UI) │                   │  (Spring Boot jar)  │
└─────────────┘                   └────────┬─────────┘
                                           │ bolt://7687
                                    ┌──────▼──────┐
                                    │   Neo4j 5.x  │
                                    │ (图谱分析数据源)│
                                    └─────────────┘
```

**重要说明**：本系统当前为 **Neo4j 单库驱动**架构。所有业务数据（科研人员、论文、专利、项目、院系）均存储于 Neo4j 并由其提供全部查询。MySQL 在架构中定位为"权威数据源"（schema.sql 与 DataSyncService 同步服务已预置），但当前版本运行**不依赖 MySQL**，可按需后置部署。

---

## 二、生产环境要求

### 2.1 硬件配置建议

| 配置档次 | CPU | 内存 | 磁盘 | 适用场景 |
|---------|-----|------|------|---------|
| 最低配置 | 2 核 | 4 GB | 40 GB | 当前演示数据规模（约 1700 节点） |
| **推荐配置** | 4 核 | 8 GB | 100 GB | 万人以下真实数据 |
| 增强配置 | 8 核 | 16 GB | 200 GB | 更大规模或高并发访问 |

> 磁盘注意事项：Neo4j 数据目录与应用日志目录建议放在非系统盘，并预留 30% 以上剩余空间。

### 2.2 操作系统

- Windows Server 2016 及以上（或 Windows 10/11）
- Linux（CentOS 7+ / Ubuntu 20.04+ / 麒麟等国产化系统）

本文档以 **Windows 为主**进行说明，Linux 差异部分单独标注。

### 2.3 端口需求

| 端口 | 用途 | 开放范围 |
|------|------|---------|
| **8089** | 应用 HTTP 服务 | 校内网段（面向最终用户） |
| 7687 | Neo4j Bolt 协议 | **仅本机**（应用与本机部署时） |
| 7474 | Neo4j 浏览器管理界面 | 仅运维管理网段（可选关闭） |
| 3306 | MySQL（如部署） | 仅本机 |

---

## 三、基础环境安装

### 3.1 安装 JDK 21

1. 下载并安装 JDK 21（Oracle JDK 21 或 OpenJDK 21 均可）；
2. 配置环境变量 `JAVA_HOME` 指向安装目录（如 `C:\Program Files\Java\jdk-21`）；
3. 验证：

```powershell
java -version
# 应输出 java version "21.x.x"
```

### 3.2 安装 Neo4j 5.x

1. 从 Neo4j 官网下载 **Neo4j Community Server 5.x** 的 zip 压缩包（本手册以 5.26 验证）；
2. 解压至规划目录（示例：`C:\neo4j-5.26`，Linux 示例：`/opt/neo4j-5.26`）；
3. 修改配置文件 `conf\neo4j.conf`，按服务器内存调整以下参数（以 8 GB 内存服务器为例）：

```properties
# JVM 堆内存（生产必须配置，不可依赖默认值）
# 建议为服务器内存的 1/4，初始与最大设为相同值，避免运行时堆扩展带来性能波动
server.memory.heap.initial_size=2048m
server.memory.heap.max_size=2048m
# 页缓存（建议为服务器内存的 1/2，用于图数据缓存）
server.memory.pagecache.size=4g

# 仅监听本机（应用与本机部署时的安全默认值）
server.default_listen_address=localhost
```

> **⚠ 重要**：必须取消 `server.memory.heap.initial_size` 和 `server.memory.heap.max_size` 的注释并设置明确值。Neo4j 默认会自动计算堆大小，与本应用的 Java 进程抢占内存，定时任务极易触发 `OutOfMemoryError`。以 8GB 内存服务器为例：Neo4j 堆 2G + 页缓存 4G + 应用 JVM 2G = 8G。

4. **设置数据库初始密码**（首次使用，命令在 bin 目录下执行）：

```powershell
.\neo4j-admin.bat dbms set-initial-password <生产密码>
```

> 密码要求：8 位以上，包含字母和数字。**切勿使用演示环境的弱密码**。

5. 启动并验证：

```powershell
.\neo4j.bat console
# 看到日志 "Bolt enabled on localhost:7687" 即启动成功，Ctrl+C 停止
```

6. **注册为 Windows 服务（生产必做，实现开机自启与崩溃恢复）**：

以管理员身份打开 PowerShell，执行：

```powershell
.\neo4j.bat windows-service install
Start-Service neo4j
```

> Linux 环境等价操作：`bin/neo4j start`（配合 systemd unit 见 8.4 节）。

### 3.3 （可选）安装 MySQL 8.x

当前版本运行不依赖 MySQL。若需启用"MySQL 权威数据源 + 同步"完整架构：

1. 安装 MySQL 8.x；
2. 执行项目内脚本 `src/main/resources/db/schema.sql` 建库建表；
3. （演示用途）执行 `src/main/resources/db/test-data.sql` 导入测试数据；
4. 修改应用配置中的 MySQL 连接密码（见 7.2 节）；
5. 调用 `GET /portrait/api/test/sync` 触发 MySQL → Neo4j 全量同步。

---

## 四、数据库初始化（Neo4j）

### 4.1 导入数据

**演示/测试数据**（500 人员 / 500 论文 / 300 专利 / 300 项目 / 133 院系；合作关系 2059 条、引用 658 条）：

```powershell
# 在 Neo4j bin 目录下执行
.\cypher-shell.bat -u neo4j -p <生产密码> -f <项目路径>\src\main\resources\db\neo4j-test-data.cypher
```

> **⚠ 版本升级提示（v1.1）**：本次数据脚本新增论文「国际合作」（`international`）与专利「成果转化」（`transferred` / `transferAmount`）字段，合作网络按学术团队聚集结构重构。**从旧版本升级时必须重新导入本脚本**（脚本含清空旧数据操作），否则「国际合作」「成果转化」雷达维度与枢纽/社群分析将无数据可算。

**真实数据**：需按相同节点标签（Researcher / Paper / Patent / Project / Institution）与关系类型（见 13.3 节数据模型）编写导入脚本，或通过 `/api/researcher/save` 等接口录入。

### 4.2 验证数据导入结果

```powershell
.\cypher-shell.bat -u neo4j -p <生产密码> "MATCH (n) RETURN labels(n)[0] AS label, count(n) AS cnt ORDER BY cnt DESC;"
```

预期结果（演示数据）：

| 标签 | 数量 |
|------|------|
| Researcher | 500 |
| Paper | 500 |
| Patent | 300 |
| Project | 300 |
| Institution | 133 |
| WeightConfig | 5 |

> 注意：测试数据脚本会**先清空**现有图数据（`MATCH (n) DETACH DELETE n`），生产库执行前请确认。

---

## 五、应用构建打包

### 5.1 前置检查（重要）

`pom.xml` 中必须包含 **spring-boot-maven-plugin**（生成可执行 fat jar 的必要条件）：

```xml
<build>
    <plugins>
        <plugin>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-maven-plugin</artifactId>
        </plugin>
    </plugins>
</build>
```

> 本项目于 2026-09-11 已补充该配置，基于此后的代码版本构建即可。

### 5.2 构建步骤

在项目根目录执行：

```powershell
.\mvnw.cmd clean package -DskipTests
```

构建成功标志（约 30 秒）：

```
[INFO] --- spring-boot:3.2.5:repackage (repackage) @ research-portrait ---
[INFO] Replacing main artifact ...target\research-portrait-1.0.0.jar with repackaged archive
[INFO] BUILD SUCCESS
```

### 5.3 构建产物

| 文件 | 大小（参考） | 用途 |
|------|------|------|
| `target\research-portrait-1.0.0.jar` | 约 49 MB | **发布用可执行 jar**（内含全部依赖与前端页面） |
| `target\research-portrait-1.0.0.jar.original` | 约 0.2 MB | 打包前的原始 jar（无用途，无需发布） |

---

## 六、部署目录结构（推荐）

在服务器上规划部署目录（示例 `D:\apps\research-portrait`）：

```
D:\apps\research-portrait\
├── research-portrait-1.0.0.jar    # 应用主程序
├── application.yaml               # 外置生产配置（覆盖 jar 内默认值）
├── logs\                           # 运行日志（应用自动创建）
└── backup\                         # Neo4j 备份输出目录
```

> **外置配置原理**：Spring Boot 会优先加载 jar 同目录下的 `application.yaml`，未包含的配置项沿用 jar 内默认值。好处：修改数据库密码、端口等无需重新打包。

---

## 七、生产配置文件

### 7.1 外置 application.yaml 完整模板

在 jar 同目录创建 `application.yaml`，内容如下（**标注【必改】的项必须修改**）：

```yaml
server:
  port: 8089                          # 应用端口（默认 8089，可按需修改）
  servlet:
    context-path: /portrait

spring:
  application:
    name: research-portrait

  # ---------- MySQL（可选，当前版本可不配置） ----------
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/research_portrait?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowMultiQueries=true
    username: root
    password: 【如启用 MySQL 则必改为生产密码】

  # ---------- Neo4j（核心数据库） ----------
  neo4j:
    uri: bolt://localhost:7687       # Neo4j 与应用同机部署时保持 localhost
    authentication:
      username: neo4j
      password: 【必改】生产密码       # 与 3.2 节第 4 步设置的 Neo4j 密码一致

mybatis:
  configuration:
    map-underscore-to-camel-case: true
  mapper-locations: classpath:mapper/*.xml

# ---------- 生产日志配置（推荐） ----------
logging:
  file:
    name: logs/portrait.log           # 日志输出文件
  logback:
    rollingpolicy:
      max-file-size: 50MB             # 单文件上限
      max-history: 30                 # 保留 30 天
      total-size-cap: 5GB             # 总量上限
  level:
    # 抑制 Spring Data Neo4j 框架内部 Cypher 的 id() 弃用警告（框架层问题，不影响功能）
    org.springframework.data.neo4j.cypher.deprecation: ERROR
    root: INFO
```

### 7.2 配置项速查

| 配置项 | 默认值 | 生产要求 |
|--------|--------|---------|
| `spring.neo4j.uri` | bolt://localhost:7687 | 分机部署时改为 `bolt://<Neo4j服务器IP>:7687` |
| `spring.neo4j.authentication.password` | 开发演示密码 | **必须修改** |
| `server.port` | 8089 | 按校内端口规划调整 |
| `logging.file.name` | （控制台输出） | **建议配置**，否则日志只在控制台 |

---

## 八、启动、停止与服务化

### 8.1 命令行启动（调试/首次验证用）

```powershell
cd D:\apps\research-portrait
java -Xms1g -Xmx2g -jar research-portrait-1.0.0.jar
```

> **⚠ 重要**：`-Xms1g -Xmx2g` 参数为**必须项**，不可省略。不指定时 Java 默认堆仅为物理内存的 1/4，与 Neo4j 同机部署时定时任务会触发 `OutOfMemoryError` 导致页面无法访问。

启动成功的标志：控制台出现 `Tomcat started on port 8089` 与 `Started ResearchPortraitApplication`。

### 8.2 停止

命令行方式按 `Ctrl+C`；后台进程方式：

```powershell
# 查找并停止监听 8089 的进程
Get-NetTCPConnection -LocalPort 8089 -State Listen | ForEach-Object { Stop-Process -Id $_.OwningProcess -Force }
```

### 8.3 Windows 服务化（生产推荐）

使用 [NSSM](https://nssm.cc/)（Non-Sucking Service Manager）将应用注册为 Windows 服务：

```powershell
# 以管理员身份执行（nssm.exe 需提前下载）
# 注意：-Xms1g -Xmx2g 为必要参数，不可省略（详见 Q7）
nssm install ResearchPortrait "C:\Program Files\Java\jdk-21\bin\java.exe" "-Xms1g -Xmx2g -jar D:\apps\research-portrait\research-portrait-1.0.0.jar"
nssm set ResearchPortrait AppDirectory "D:\apps\research-portrait"
nssm set ResearchPortrait AppStdout "D:\apps\research-portrait\logs\service-out.log"
nssm set ResearchPortrait AppStderr "D:\apps\research-portrait\logs\service-err.log"
nssm start ResearchPortrait
```

服务化后可实现：开机自启、崩溃自动拉起、`services.msc` 图形化管理。

### 8.4 Linux 服务化（systemd）

创建 `/etc/systemd/system/research-portrait.service`：

```ini
[Unit]
Description=Research Portrait Application
After=network.target neo4j.service

[Service]
Type=simple
User=appuser
WorkingDirectory=/opt/apps/research-portrait
ExecStart=/usr/bin/java -Xms1g -Xmx2g -jar research-portrait-1.0.0.jar
Restart=on-failure
RestartSec=10

[Install]
WantedBy=multi-user.target
```

```bash
systemctl daemon-reload
systemctl enable --now research-portrait
```

> **启动顺序要求**：应用虽在 Neo4j 未就绪时也能启动（驱动懒连接、自动重连），但为保证首次访问即可用，生产环境应确保 Neo4j 服务先于应用启动（上方 `After=neo4j.service` 已处理）。

---

## 九、上线验证清单

部署完成后按顺序执行以下检查（全部通过方可交付）：

| # | 验证项 | 操作 | 预期结果 |
|---|--------|------|---------|
| 1 | 应用进程 | 访问 `http://<IP>:8089/portrait/api/test/hello` | 返回"科研人员画像系统启动成功" |
| 2 | 图谱数据 | 访问 `http://<IP>:8089/portrait/api/graph/full` | 返回 JSON，节点数/关系数与 4.2 节一致 |
| 3 | 前端页面 | 浏览器打开 `http://<IP>:8089/portrait/` | 显示系统主界面与知识图谱 |
| 4 | 画像功能 | 单击任一蓝色节点 | 弹出人员画像窗口，含智能标签与项目层级分布 |
| 5 | 人才发现 | 点击"人才发现" | 高端人才筛选视图正常加载 |
| 6 | 群体对比 | 点击"群体对比"，切换划分维度与对比指标 | 柱状图与年度趋势图正常渲染 |
| 7 | 机构对标 | 点击"院系对标" | 院系对比 7 维雷达图正常 |
| 8 | 维度钻取 | 院系对标 → 点击任一维度按钮 | 弹出钻取面板（计算方式 + 二级指标 + 原始成果清单） |
| 9 | 竞争力简报 | 院系对标 → 勾选 ≥2 个院系 → "生成分析简报（PDF）" | 下载 PDF 简报（含优势/短板/建议） |
| 10 | 合作网络分析 | 画像弹窗 → 合作网络 → "在主图谱中探查二级合作网络" | 枢纽学者金色高亮、社群节点按颜色区分 |
| 11 | Excel 导出 | 点击"导出报表" | 下载 xlsx 文件且中文无乱码 |
| 12 | PDF 报告导出 | 单击科研人员节点 → 画像弹窗点击"下载 PDF 报告" | 下载 pdf 文件且中文显示正常 |
| 13 | 日志落盘 | 查看 `logs/portrait.log` | 文件存在且持续写入 |
| 14 | 重启自愈 | 重启服务器 | Neo4j 与应用服务自动恢复运行 |

---

## 十、安全加固

1. **修改 Neo4j 默认/演示密码**（最关键）：按 3.2 节第 4 步设置强密码，并同步更新 `application.yaml`；
   - 已有数据需改密时：`cypher-shell -u neo4j -p 旧密码 "ALTER CURRENT USER SET PASSWORD FROM '旧密码' TO '新密码';"`
2. **网络隔离**：Neo4j 的 7474/7687 端口不应对最终用户开放（配置 `server.default_listen_address=localhost` 即仅本机监听）；
3. **防火墙规则**（仅开放应用端口）：

```powershell
netsh advfirewall firewall add rule name="ResearchPortrait-HTTP" dir=in action=allow protocol=TCP localport=8089
```

4. **移除调试接口（可选加固）**：`/api/test/sync`（数据全量同步）与 `/api/test/hello` 为开发调试接口，生产如不需要可在 TestController 上移除或限制；
5. **MySQL（如启用）**：不使用 root 账号、仅本机监听、与应用同步修改强密码。

---

## 十一、备份与恢复

### 11.1 Neo4j 备份（停机方式，适合每日定时）

```powershell
# 停止 Neo4j 服务后执行（bin 目录下）
neo4j-admin database dump neo4j --to-path=D:\apps\research-portrait\backup
```

生成形如 `neo4j-2026-09-11T03-00-00.dump` 的备份文件。建议通过 Windows 任务计划每日低峰执行，并定期拷贝至异地存储。

### 11.2 Neo4j 恢复

```powershell
# 停止 Neo4j 服务后执行
neo4j-admin database load neo4j --from-path=D:\apps\research-portrait\backup\<备份文件>.dump --overwrite-true
```

### 11.3 其他备份对象

| 对象 | 位置 | 建议 |
|------|------|------|
| 应用 jar 与外置配置 | 部署目录 | 每次版本发布后归档 |
| 数据初始化/真实数据脚本 | `src/main/resources/db/` | 纳入版本管理 |
| 日志 | `logs/` | 按滚动策略保留即可 |

---

## 十二、常见问题（FAQ）

**Q1：启动报 "Port 8089 was already in use"？**
存在旧实例占用端口。排查与停止命令：

```powershell
Get-NetTCPConnection -LocalPort 8089 -State Listen | Select-Object OwningProcess
Get-Process -Id <上一步的PID>   # 确认是本系统的 java 进程后再停止
Stop-Process -Id <PID> -Force
```

**Q2：应用已启动，但页面查询报错、图谱空白？**
大概率是 Neo4j 未启动或密码不匹配。验证：`Test-NetConnection localhost -Port 7687`。注意：应用启动时不校验 Neo4j 连接（懒连接），Neo4j 后启动也能自动恢复，但启动前访问会报错。

**Q3：`mvnw package` 出来的 jar 无法 `java -jar` 运行？**
检查 pom.xml 是否包含 spring-boot-maven-plugin（见 5.1 节）。缺少该插件时只会生成普通 jar。

**Q4：日志出现 id() 函数弃用警告（WARN）？**
属 Spring Data Neo4j 框架内部行为，功能不受影响，保持 7.1 节模板中的日志级别配置即可屏蔽。

**Q5：Excel 导出文件名或内容中文乱码？**
确保服务器 JVM 使用 UTF-8：启动命令添加 `-Dfile.encoding=UTF-8`。

**Q6：电脑/服务器重启后系统无法访问？**
检查两个服务是否设置为自启：Neo4j Windows 服务（`Start-Service neo4j`）与应用服务（nssm/systemd 方式自动拉起）。

**Q7：运行一段时间后页面突然刷不出来（日志含 `OutOfMemoryError: Java heap space`）？**
这是 JVM 堆内存不足导致的。常见原因：
1. 启动命令未指定 `-Xms1g -Xmx2g` 参数（Java 默认堆仅为物理内存的 1/4，与 Neo4j 同机时尤其不够）；
2. Neo4j 的 `neo4j.conf` 未配置堆大小，自动计算后与 Java 应用抢占内存。

**解决步骤**：
- 确认应用启动命令包含 `java -Xms1g -Xmx2g -jar research-portrait-1.0.0.jar`（nssm 服务需重新注册并带该参数）；
- 确认 `neo4j.conf` 中取消注释并设置了 `server.memory.heap.initial_size` 和 `server.memory.heap.max_size`（见 3.2 节）；
- 重启应用和 Neo4j 服务后验证。

---

## 十三、附录

### 13.1 关键文件清单

| 文件 | 说明 |
|------|------|
| `research-portrait-1.0.0.jar` | 应用可执行程序 |
| `src/main/resources/application.yaml` | 开发默认配置（生产使用外置配置覆盖） |
| `src/main/resources/db/schema.sql` | MySQL 建库脚本（可选启用） |
| `src/main/resources/db/neo4j-test-data.cypher` | Neo4j 演示数据脚本 |
| `src/main/resources/db/generate_test_data.py` | 演示数据生成脚本（Python） |

### 13.2 REST 接口清单（前缀均为 `/portrait`）

| 接口 | 方法 | 功能 |
|------|------|------|
| `/api/test/hello` | GET | 存活检查 |
| `/api/test/sync` | GET | 触发 MySQL→Neo4j 全量同步（调试用） |
| `/api/graph/full` | GET | 图谱全量数据（节点+关系） |
| `/api/graph/expand/{nodeId}` | GET | 展开节点邻居 |
| `/api/graph/cooperation/{id}` | GET | 合作网络查询 |
| `/api/graph/cooperation-network/{id}` | GET | 二级合作网络探查（含枢纽学者与学术社群分析） |
| `/api/researcher/get/{id}` | GET | 人员详情 |
| `/api/researcher/all` | GET | 人员列表 |
| `/api/researcher/profile/{id}` | GET | 人员画像（含趋势/主题/合作分析） |
| `/api/researcher/report/{id}` | GET | PDF 分析报告下载 |
| /api/researcher/search | POST | 高级人才检索 |
| `/api/researcher/export` | GET | Excel 报表导出 |
| `/api/researcher/save` / `delete/{id}` | POST/DELETE | 人员维护 |
| `/api/institution/all` | GET | 院系列表 |
| `/api/institution/radar/{id}` | GET | 院系竞争力雷达数据 |
| `/api/institution/compare` | POST | 多院系对标 |
| `/api/institution/drill/{id}?dimension=xxx` | GET | 雷达维度钻取（二级指标 + 计算方式 + 原始成果清单） |
| `/api/institution/brief-pdf?ids=1,2,3` | GET | 机构竞争力分析简报 PDF 下载 |
| `/api/analysis/group-compare?dimension=discipline` | GET | 多维度群体对比（学科/职称/院系/人才类别） |
| `/api/paper/all`、`/api/patent/all`、`/api/project/all` | GET | 成果列表查询 |
| `/api/paper/save`、`/api/patent/save`、`/api/project/save` | POST | 成果数据维护 |

### 13.3 图数据模型（Neo4j）

**节点**：Researcher（科研人员）、Paper（论文，含 international 国际合作标识）、Patent（专利，含 transferred 转化标识与 transferAmount 转化金额）、Project（项目）、Institution（院系，含 parentId 二级结构）、WeightConfig（学科权重配置）

**关系**：

| 关系类型 | 方向 | 含义 | 演示数据量 |
|---------|------|------|-----------|
| BELONG_TO | 人员→院系 | 隶属 | 500 |
| WRITE | 人员→论文 | 论文撰写 | 1159 |
| INVENT | 人员→专利 | 专利发明 | 619 |
| CHARGE | 人员→项目 | 项目负责 | 300 |
| PARTICIPATE | 人员→项目 | 项目参与 | 684 |
| COOPERATE_WITH | 人员↔人员 | 科研合作 | 2059 |
| CITES | 论文→论文 | 论文引用 | 658 |

---

> 本手册中全部命令与流程均已于 2026-09-11 在实际环境中验证（构建 27 秒、jar 49 MB、独立运行、图谱接口 0.4 秒响应）；2026-09-18 数据脚本升级至 v1.1（新增国际合作/成果转化字段、团队聚集合作结构）。
